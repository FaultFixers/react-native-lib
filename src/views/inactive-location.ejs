<%- include('partials/global-header') %>

<%- include('partials/page-header', {title: 'Tickets'}) %>

<div class="page-content">
    <div class="bordered-box clearfix">
        <img src="<%= squareLogo.compressedUrl %>" class="side-logo" />

        <h2><%= building.name %></h2>

        <h3><%= location.name %></h3>

        <p>This location is not in use at the moment. If you think this is a mistake, please contact your building manager.</p>

        <%- include('partials/ticket-list', {tickets, showNoTicketsMessage: false}) %>
    </div>
</div>

<%- include('partials/copyright-footer') %>
